<table class="table table-striped table-bordered">
    <thead>
      <tr>
        <th>Word</th>
        <th>Frequency</th>
      </tr>
    </thead>
      <% JSON.parse(document.word_frequency)["word_frequency"].each do |elem| %>
        <tr><td class="frequency_item"><%= elem[0].capitalize %></td><td class="frequency_item">  <%= elem[1] %></td></tr>
      <% end %>
 </table>